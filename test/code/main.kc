const int kuba = 1E6
var any[] kubaArr = [1, "Hello"]

const_func str myStr(any value, int index):
    return str(index) + ": " + str(value)

const_func void main(const str a, const bool b, const arr c, const obj<Object> d, const any e, const int f):
    Output.log(a, b ? "Yes" : "No", c.join("X"), d, e, f, kuba)
    Output.log(str(kubaArr, myStr))
    kubaArr.push("Kuba is", 0)
    Output.log(str(kubaArr, myStr))
    ##Kuba
    #*Hello
    Jacob*#
    ##Ahoj1
    ##Ahoj2
    ##Ahoj3
    ##Ahoj4
    return "An error should be there, because main func is void."
